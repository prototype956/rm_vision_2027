schema_version: 2
# auto：优先选择已编译且可用的 TensorRT 后端，其次选择 OpenVINO GPU。
backend: openvino
model_path: src/modules/armor_detector/models/0526.onnx
device: GPU
enemy_color: blue
confidence_threshold: 0.65
nms_iou_threshold: 0.45
