schema_version: 2
# auto: prefer an available compiled TensorRT backend, then OpenVINO GPU.
backend: openvino
model_path: src/modules/armor_detector/models/0526.onnx
device: GPU
enemy_color: blue
confidence_threshold: 0.65
nms_iou_threshold: 0.45
