schema_version: 1

board:
  columns: 11
  rows: 8
  square_size_mm: 25.0

# 采集阶段仅保存原图；离线 solve 逐张弹窗，按任意键确认后按完整棋盘检测接纳样本。
# 清晰度和倾斜比仅作为诊断记录，不设验收门限。
capture:
  min_samples: 20

quality:
  max_rms_px: 0.5
  max_view_rms_px: 1.0
  min_area_ratio: 2.0

output_dir: artifacts/camera_calibration
