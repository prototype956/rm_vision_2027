schema_version: 1

device:
  index: 0

output:
  width: 1280
  height: 720
  pixel_format: bgr8

roi:
  centered: true

exposure:
  auto: false
  time_us: 10000

capture:
  timeout_ms: 100
  time_offset_ms: 0  # 加到软件映射的采集时刻；曝光相位待实机确认。
